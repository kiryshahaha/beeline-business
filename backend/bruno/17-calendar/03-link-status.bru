meta {
  name: Статус ссылки
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/schedule/calendar/token
  body: none
  auth: bearer
}

auth:bearer {
  token: {{worker_access_token}}
}

assert {
  res.status: eq 200
  res.body.active: eq true
}
