meta {
  name: Несуществующий офис — 404
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/v1/schedule?office_id=2147483647
  body: none
  auth: bearer
}

params:query {
  office_id: 2147483647
}

auth:bearer {
  token: {{observer_access_token}}
}

assert {
  res.status: eq 404
  res.body.detail: eq "Офис не найден"
}
