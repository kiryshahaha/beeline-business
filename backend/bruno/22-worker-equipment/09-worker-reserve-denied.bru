meta {
  name: Инженер не меняет норму резерва
  type: http
  seq: 9
}

put {
  url: {{base_url}}/api/v1/offices/{{office_id}}/kit-reserve/{{created_appliance_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{worker_access_token}}
}

body:json {
  {"quantity":1}
}

assert {
  res.status: eq 403
}
