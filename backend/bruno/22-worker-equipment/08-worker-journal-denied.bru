meta {
  name: Инженеру журнал недоступен
  type: http
  seq: 8
}

get {
  url: {{base_url}}/api/v1/equipment/operations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{worker_access_token}}
}

assert {
  res.status: eq 403
}
