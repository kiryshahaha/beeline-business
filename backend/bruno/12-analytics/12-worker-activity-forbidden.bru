meta {
  name: Исполнителю лента недоступна — 403
  type: http
  seq: 12
}

get {
  url: {{base_url}}/api/v1/analytics/recent-activity
  body: none
  auth: bearer
}

auth:bearer {
  token: {{worker_access_token}}
}

assert {
  res.status: eq 403
}
