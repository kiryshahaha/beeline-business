meta {
  name: Получить офис по ID
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/offices/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{observer_access_token}}
}

assert {
  res.status: eq 200
  res.body.id: eq 1
}
