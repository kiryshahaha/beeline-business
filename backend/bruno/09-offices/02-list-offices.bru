meta {
  name: Получить список офисов
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/offices/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{observer_access_token}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
