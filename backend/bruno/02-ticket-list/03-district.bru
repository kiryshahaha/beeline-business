meta {
  name: По выбранному району
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/tickets?district_id={{district_id}}
  body: none
  auth: none
}

params:query {
  district_id: {{district_id}}
}

assert {
  res.status: eq 200
}

tests {
  test("Каждая заявка соответствует заданным фильтрам", function () {
    res.getBody().forEach(function (ticket) {
      expect(ticket.location.district_id).to.equal(Number(bru.getVar("district_id")));
    });
  });

  test("Размер страницы не превышает лимит", function () {
    expect(res.getBody().length).to.be.at.most(20);
  });
}

docs {
  Район можно указать без города. Local.district_id — выбранный район, по умолчанию Невский в новой демобазе.
}
