meta {
  name: worker-import-denied
  type: http
  seq: 9
}

post {
  url: {{base_url}}/api/v1/data/import?dry_run=false
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{worker_access_token}}
}

body:multipart-form {
  file: @file(fixtures/dataset.zip)
}

assert {
  res.status: eq 403
}
