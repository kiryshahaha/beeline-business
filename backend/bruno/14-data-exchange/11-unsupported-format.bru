meta {
  name: unsupported-format
  type: http
  seq: 11
}

get {
  url: {{base_url}}/api/v1/data/export?format=pdf
  body: none
  auth: bearer
}

auth:bearer {
  token: {{observer_access_token}}
}

assert {
  res.status: eq 422
}
