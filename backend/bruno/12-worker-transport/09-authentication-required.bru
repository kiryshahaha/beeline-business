meta {
  name: authentication-required
  type: http
  seq: 9
}

patch {
  url: {{base_url}}/api/v1/users/{{worker_id}}
  body: json
  auth: none
}

body:json {
  {
    "worker_profile": {
      "transport_type": "car"
    }
  }
}

assert {
  res.status: eq 401
}
