meta {
  name: Нулевой ID района — 422
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/tickets?service_area_id=0
  body: none
  auth: bearer
}

auth:bearer {
  token: {{observer_access_token}}
}

params:query {
  service_area_id: 0
}

assert {
  res.status: eq 422
}

tests {
  test("Ошибка относится к параметру service_area_id", function () {
    const errors = res.getBody().detail;
    expect(errors).to.be.an("array").and.not.empty;
    expect(errors.some(function (error) {
      return error.loc[0] === "query" && error.loc[1] === "service_area_id";
    })).to.equal(true);
  });
}

docs {
  ID района должен быть положительным целым числом.
}
