meta {
  name: get-own-route
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/routes/{{saved_route_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{worker_access_token}}
}

assert {
  res.status: eq 200
}
