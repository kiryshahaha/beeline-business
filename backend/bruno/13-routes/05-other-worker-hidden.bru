meta {
  name: other-worker-hidden
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/v1/routes/{{saved_route_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{worker_two_access_token}}
}

assert {
  res.status: eq 404
}
