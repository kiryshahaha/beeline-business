meta {
  name: Политика доступна наблюдателю, не исполнителю
  type: http
  seq: 16
}
get {
  url: {{base_url}}/api/v1/planning/policy
  auth: bearer
}
auth:bearer {
  token: {{worker_access_token}}
}
assert {
  res.status: eq 403
}
