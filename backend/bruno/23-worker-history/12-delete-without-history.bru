meta {
  name: Пользователь без истории удаляется
  type: http
  seq: 12
}

delete {
  url: {{base_url}}/api/v1/users/{{history_temp_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{observer_access_token}}
}

assert {
  res.status: eq 204
}
