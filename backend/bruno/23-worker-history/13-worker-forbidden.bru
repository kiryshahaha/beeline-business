meta {
  name: Исполнитель не архивирует других
  type: http
  seq: 13
}

post {
  url: {{base_url}}/api/v1/users/{{history_worker_id}}/archive
  body: none
  auth: bearer
}

auth:bearer {
  token: {{worker_access_token}}
}

assert {
  res.status: eq 403
}
