meta {
  name: Исполнитель не скачивает отчёт
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/v1/reports/tickets/export?format=csv
  body: none
  auth: bearer
}

auth:bearer {
  token: {{worker_access_token}}
}

assert {
  res.status: eq 403
}
