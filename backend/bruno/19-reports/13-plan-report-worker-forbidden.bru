meta {
  name: Исполнитель не скачивает отчёт по плану
  type: http
  seq: 13
}

get {
  url: {{base_url}}/api/v1/reports/plans/{{planning_plan_id}}/export
  body: none
  auth: bearer
}

auth:bearer {
  token: {{worker_access_token}}
}

assert {
  res.status: eq 403
}
