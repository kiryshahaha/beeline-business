meta {
  name: Отозванная ссылка — 404
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/v1/schedule/calendar.ics?token={{calendar_token}}
  body: none
  auth: none
}

params:query {
  token: {{calendar_token}}
}

assert {
  res.status: eq 404
}
