meta {
  name: Наблюдателю ссылка недоступна — 403
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/v1/schedule/calendar/token
  body: none
  auth: bearer
}

auth:bearer {
  token: {{observer_access_token}}
}

assert {
  res.status: eq 403
}
