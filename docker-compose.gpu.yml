# Ollama на видеокарте NVIDIA. Нужен NVIDIA Container Toolkit на хосте.
# docker compose -f docker-compose.yml -f docker-compose.gpu.yml up --build
services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
